<%= @student.name %>
<%= @student.addresses.each do |a| %>
  <%= a.description %>
  <%= a.street %>
  <%= a.city %>
  <%= a.state %>
  <%= a.zip %>
<% end %>

<%= @student.courses.each do |c| %>
  <%= c.name %>
<% end %>
